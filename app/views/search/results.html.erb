<h2>Search Results for "<%= @query %>"</h2>

<% if @results.any? %>
  <ul>
    <% @results.each do |result| %>
      <li><%= link_to result.title, project_path(result) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No results found.</p>
<% end %>
